apiVersion: ark.mckinsey.com/v1alpha1
kind: Model
metadata:
  name: memory-test-model
spec:
  type: azure
  model:
    value: gpt-4o-mini
  config:
    azure:
      baseUrl:
        value: ($azure.url)
      apiKey:
        valueFrom:
          secretKeyRef:
            name: memory-test-model-token
            key: token
      apiVersion:
        value: "2024-02-01"