apiVersion: v1
kind: Secret
metadata:
  name: mcp-config
type: Opaque
stringData:
  mcp.json: |
    {
      "mcpServers": {
        "filesystem": {
          "command": "npx",
          "args": ["-y", "@anthropic-ai/filesystem-mcp", "/tmp"]
        }
      }
    }
