# Notes

Inputs on architecture

Decisions:

1. MCP runs on a separate port.

2. `q-` is fine.

3. Ark Cluster Memory will have its own role, similar to ark api service, it will have permissions on all ark resources in the namespace and essentially the same structure as the ark api role
